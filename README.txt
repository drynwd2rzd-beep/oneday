OneDay v4.3

本次修改：
- “值得留下”改为首页直接输入框：只负责输入，不再在首页重复展示当天已经写过的内容；保存后内容进入“回顾”。
- “恢复初始设置”拆分为两个独立选项：
  1. 恢复主题与设置（主题模式、主题色、每日文案设置）
  2. 恢复默认头像
  两者均不影响事项、计划、回顾记录和统计数据。
- 统计增加“近 7 天 / 近 30 天”切换，30 天模式自动调整柱宽与日期标签密度。
- 完成概览继续只保留“累计完成”和“连续坚持”。
- 修复旧数据兼容中的 uuid / uid 错误，避免新增或兼容旧数据时因 ID 生成函数名称错误而失败。

自检：
- JavaScript 已通过 Node 语法检查。
- 已检查所有 HTML 内联 onclick 调用都有对应函数。
- 已检查关键 ID（快速记录、统计周期、恢复设置）均存在且函数引用一致。
- 已确认首页不再存在 notePreview 历史记录展示。
- 已确认恢复主题与设置、恢复头像均不会清空任务、计划、记录数据。

使用：解压后直接打开 index.html。
